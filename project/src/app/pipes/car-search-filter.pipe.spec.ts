import { CarSearchFilterPipe } from './car-search-filter.pipe';

describe('CarSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CarSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
